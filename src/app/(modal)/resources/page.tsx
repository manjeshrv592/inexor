const ResourcesPage = () => {
  // This page is now handled entirely by the layout component
  // The layout manages blog selection via state instead of URL routing
  return null;
};

export default ResourcesPage;
